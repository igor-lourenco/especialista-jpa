<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xmlns:jsr107='http://www.ehcache.org/v3/jsr107'
        xsi:schemaLocation="
			http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
        	http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">

    <!-- Documentação do ehcache
           - https://www.ehcache.org/documentation/
    -->

    <service>
        <jsr107:defaults default-template="padrao" />
    </service>

    <!-- Configuracao especifico para a entidade Pedido -->
    <cache alias="com.especialista.jpa._7_mapeandoEntidadesParaGeracaoDeDDL.modelos.Pedido" uses-template="padrao">
        <expiry>
            <!-- Tempo de vida que o objeto pode ficar no cache sem ser utilizado, ou seja, se ficar mais de 2 seg se ser utilizado sera removido do cache  -->
            <tti unit="seconds">2</tti>
        </expiry>
    </cache>

    <!-- template padrao para ser usado    -->
    <cache-template name="padrao">
        <key-type>java.lang.Object</key-type>
        <value-type>java.lang.Object</value-type>

        <expiry>
            <!-- Tempo de vida que o objeto pode ficar no cache  -->
            <ttl unit="seconds">20</ttl>
        </expiry>

        <!-- Quantidade maxima de objetos que pode ficar na memoria -->
        <heap unit="entries">1000</heap>
    </cache-template>
</config>